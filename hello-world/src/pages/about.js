import React from "react"
import Header from "../components/header"

export default () => (
  <div style={{ color: `teal` }}>
    <Header headerText="About"/>
    <Header headerText="It's pretty good."/>
    <p>Such wow. Very React.</p>
  </div>
)